//function new series
//variable inside function call local scope
//we can declare it as a global when we want to use it everywhere in our class

//normal function
let x = function(x,y){
    return x + y;
}

console.log(x(100,200));